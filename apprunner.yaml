version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm ci
      - npm run build

run:
  command: npm run start:standalone
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: APP_DATA_DIR
      value: /tmp/ozeki-data
