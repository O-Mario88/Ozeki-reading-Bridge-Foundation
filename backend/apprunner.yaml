version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
      - python manage.py collectstatic --noinput
run:
  command: gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000}
  network:
    port: 8000
    env: PORT
